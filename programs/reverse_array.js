const arr = [1, 2, 3, 4, 5, 6]
const reverse_array = arr.reverse();
console.log(reverse_array);

// In the case of string we will be do reverse to this.

const str = "this is String";
const arr3 = str.split('').reverse().join('');

console.log(arr3);
//const reverse_str = str.